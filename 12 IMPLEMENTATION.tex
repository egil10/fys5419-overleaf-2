\section{Implementation and Datasets}\label{section:impl}

\subsection{Code structure}\label{subsec:codestructure}

The implementation is a small collection of pure-NumPy / SciPy modules, written from scratch rather than on top of a high-level VQA library so that every conventional choice (sign of the cost, qubit ordering, scaling of the approximation ratio) is explicit and auditable. The package layout mirrors the pipeline of \cref{section:methods}.

\paragraph{Modules.} The \emph{data layer} (\texttt{data.py}) loads cached daily-close Parquet files and assembles $(\boldsymbol{\mu},\boldsymbol{\Sigma})$. The \emph{problem layer} (\texttt{portfolio.py}, \texttt{ising.py}) fixes the canonical cost $C(\boldsymbol{x})$ via the \texttt{PortfolioProblem} dataclass and the \texttt{DEFAULTS} dictionary, and performs the $x_i = (1-z_i)/2$ substitution to build the diagonal $H_C$ and the sparse mixer $H_M$. The \emph{solver layer} (\texttt{qaoa.py}, \texttt{optimize.py}, \texttt{classical.py}) implements the alternating ansatz with its $\langle\psi|H_C|\psi\rangle$ energy, the classical outer loop, and the brute-force, greedy-Sharpe, Markowitz-then-round and simulated-annealing baselines behind a uniform \texttt{SolverResult} type. The \emph{orchestration layer} (\texttt{metrics.py}, \texttt{compare.py}, \texttt{plotting.py}) computes the approximation ratio with $p_{\text{opt}}$ and $p_{\text{feas}}$, and drives the figures of \cref{section:result}.

\paragraph{Reproducibility.} Dependencies are pinned in \texttt{requirements.txt} to \texttt{numpy>=1.26,<3}, \texttt{pandas>=2,<3}, \texttt{scipy>=1.11,<2}, \texttt{matplotlib>=3.7,<4}, \texttt{yfinance>=0.2.40,<1}, \texttt{pyarrow>=14,<22} and \texttt{pytest>=8,<9}, with Python 3.11 as the target interpreter; the project ran without modification both on a local 3.11.x environment and on Colab.

The numerical heart of the simulator is short enough to display in full. Because $H_C$ is diagonal in the computational basis it is stored as a length-$2^n$ vector and applied by elementwise multiplication; because the X-mixer factorises as $\exp(-i\beta\sum_i X_i) = \prod_i \exp(-i\beta X_i)$, each layer is applied as $n$ independent single-qubit rotations on a $(2,\dots,2)$ tensor reshape rather than by diagonalising a $2^n\times 2^n$ matrix.

\begin{lstlisting}[language=Python,style=python,caption={Core QAOA layer (pure NumPy, $\mathcal{O}(n\cdot 2^n)$ per layer).},label={lst:qaoa-core}]
def _apply_X_mixer(psi, beta, n):
    cos  = np.cos(beta)
    isin = -1j * np.sin(beta)
    psi  = psi.reshape((2,) * n)
    for i in range(n):
        psi = np.moveaxis(psi, i, 0)
        new = np.empty_like(psi)
        new[0] = cos * psi[0] + isin * psi[1]
        new[1] = isin * psi[0] + cos * psi[1]
        psi = np.moveaxis(new, 0, i)
    return psi.reshape(-1)

def qaoa_statevector(gammas, betas, H_C_diag, n):
    dim = 1 << n
    psi = np.ones(dim, dtype=complex) / np.sqrt(dim)
    for gamma, beta in zip(gammas, betas):
        psi = np.exp(-1j * gamma * H_C_diag) * psi
        psi = _apply_X_mixer(psi, float(beta), n)
    return psi
\end{lstlisting}

\paragraph{Hybrid loop.} The simulator returns the variational energy $E(\boldsymbol{\gamma},\boldsymbol{\beta}) = \langle\psi(\boldsymbol{\gamma},\boldsymbol{\beta})|H_C|\psi(\boldsymbol{\gamma},\boldsymbol{\beta})\rangle$ for a given parameter point, and SciPy's COBYLA --- a gradient-free trust-region method with \texttt{maxiter}\,$=200$ and initial step \texttt{rhobeg}\,$=0.1$ --- updates the $2p$ angles \parencite{zhou2018qaoa}. Each call to \texttt{solve} performs ten random restarts $(\boldsymbol{\gamma}_0,\boldsymbol{\beta}_0)$ to mitigate convergence to local minima of the non-convex landscape \parencite{farhi2014qaoa}, parallelised over a \texttt{ThreadPoolExecutor} (both SciPy's C-level optimiser and the NumPy kernels release the GIL, giving a near-linear speed-up on a multicore laptop). Statevector simulation removes shot noise entirely, so any non-monotonicity in $p$ that appears in \cref{section:result} is attributable to the optimiser, not to sampling variance.

\subsection{Datasets}\label{subsec:datasets}

All price data is daily adjusted close from \texttt{yfinance}, cached locally as Parquet. Four thematic baskets feed the experiments, summarised in \cref{tab:datasets}. Log-returns are computed as $r_i(t) = \log(P_i(t+1)/P_i(t))$, the expected-return vector $\mu_i = \langle r_i\rangle_t$ as the time-mean, and the covariance $\Sigma_{ij} = \mathrm{cov}(r_i, r_j)$ as the sample covariance over the same window.

\begin{table*}[t]
  \centering
  \small
  \caption{Datasets used throughout \cref{section:result}. All four baskets share the project-wide universe window 2023-01-03 to 2025-12-30 (751 trading days), set once in \texttt{scripts.data.UNIVERSE\_START}/\texttt{\_END} so that every basket and every concatenated $n$-asset instance uses the same in-sample period.}
  \label{tab:datasets}
  \begin{tabular}{@{}llll@{}}
    \toprule
    Basket & Tickers & Theme & Window \\
    \midrule
    \texttt{mag7}         & AAPL, MSFT, GOOGL, AMZN & Mega-cap tech --- high correlation, low dispersion       & 2023-01 -- 2025-12 \\
    \texttt{anti}         & XOM, DAL, NEM, JPM      & Macro-opposed (oil, airline, gold, bank)                   & 2023-01 -- 2025-12 \\
    \texttt{quantum\_big} & IBM, HON, ACN, NVDA     & Established firms with quantum exposure                    & 2023-01 -- 2025-12 \\
    \texttt{quantum}      & IONQ, RGTI, QBTS, QUBT  & Quantum-computing pure-plays --- speculative, high vol     & 2023-01 -- 2025-12 \\
    \bottomrule
  \end{tabular}
\end{table*}

The four baskets are deliberately heterogeneous in volatility and correlation structure: \texttt{mag7} is a tightly co-moving block of tech mega-caps; \texttt{anti} (energy, airline, gold, bank) is a low-correlation contrast set; \texttt{quantum\_big} mixes large-cap quantum-adjacent names; and \texttt{quantum} is a high-volatility pure-play quantum basket. This range lets us check that QAOA's behaviour is not an artefact of a single covariance regime. The canonical $n=16$ instance used throughout \cref{section:result} is exactly the concatenation of the four baskets in the order \texttt{mag7}, \texttt{quantum}, \texttt{quantum\_big}, \texttt{anti}, and the brute-force optimum of the canonical problem $(K=4,\lambda=2.0,A=0.5)$ selects $\{\text{GOOGL},\text{IBM},\text{NVDA},\text{JPM}\}$ --- one mega-cap, one quantum-adjacent, one quantum proxy, and one macro-opposed bank, an economically diversified quartet rather than a top-Sharpe corner solution.

Per-ticker annualised statistics over the shared window are listed in \cref{tab:assets}. NVDA dominates the universe on a risk-adjusted basis ($\mathrm{Sharpe}=1.74$), with GOOGL ($1.41$) and JPM ($1.38$) close behind, while the quantum pure-plays trade extreme realised returns against extreme volatility (RGTI annualises at $143\%$ vol). The $\{\text{GOOGL},\text{IBM},\text{NVDA},\text{JPM}\}$ optimum is consistent with this ranking, but the macro-opposed JPM is preferred over a fourth tech name because the $\lambda \boldsymbol{x}^\top\boldsymbol{\Sigma}\boldsymbol{x}$ term penalises the high pairwise correlation inside \texttt{mag7}.

\begin{table}[H]
  \centering
  \small
  \caption{Annualised return, volatility and Sharpe ratio for the 16-asset universe over 2023-01-03 to 2025-12-30 (751 daily observations).}
  \label{tab:assets}
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Ticker & Ann.\ Ret.\ & Ann.\ Vol.\ & Sharpe \\
    \midrule
    AAPL  &  26.74\% &  25.46\% & 1.050 \\
    MSFT  &  24.66\% &  23.13\% & 1.066 \\
    GOOGL &  42.55\% &  30.24\% & 1.407 \\
    AMZN  &  33.49\% &  31.86\% & 1.051 \\
    IONQ  &  86.43\% & 100.05\% & 0.864 \\
    RGTI  & 115.15\% & 143.41\% & 0.803 \\
    QBTS  &  97.54\% & 146.41\% & 0.666 \\
    QUBT  &  64.62\% & 139.01\% & 0.465 \\
    IBM   &  29.15\% &  23.70\% & 1.230 \\
    HON   &   1.22\% &  20.59\% & 0.059 \\
    ACN   &   1.51\% &  25.14\% & 0.060 \\
    NVDA  &  86.48\% &  49.64\% & \textbf{1.742} \\
    XOM   &   7.75\% &  22.66\% & 0.342 \\
    DAL   &  26.27\% &  38.45\% & 0.683 \\
    NEM   &  26.92\% &  37.06\% & 0.726 \\
    JPM   &  31.81\% &  23.07\% & 1.379 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Hyperparameters and conventions}\label{subsec:hyperparams}

Unless a figure caption says otherwise, every experiment uses the canonical instance: $n=16$ assets (the concatenation of all four baskets), target cardinality $K=4$, risk-aversion $\lambda=2.0$, budget penalty $A=0.5$, and circuit depth $p=3$. These are the values exported from \texttt{portfolio.DEFAULTS} so that every notebook agrees by construction.

The classical optimiser is COBYLA with \texttt{maxiter}\,$=200$ and \texttt{rhobeg}\,$=0.1$, run from ten random multi-starts; SPSA is available as an alternative for noisy objectives but is not used in the reported figures. Initial parameters are drawn as $\gamma_k,\beta_k \sim \mathcal{U}[0, 2\pi]$ for all $k=1,\dots,p$. Because $\exp(-i\beta\sum_i X_i)$ is $2\pi$-periodic in $\beta$ up to a global phase, using a single common sampling range is equivalent to the more conventional $\beta\in[0,\pi]$ choice and keeps the multi-start sampler uniform.

Seeding follows a single master seed of $42$; restart $r$ for SPSA uses seed $42+r$, while COBYLA is deterministic given its initial point and therefore needs no additional seed. For the assets-vs-runtime scaling sweep in \cref{fig:app_scaling} we vary the number of random subsets per problem size as $M(n) = 20$ for $n\leq 10$, $M(12)=8$, $M(14)=4$ and $M(16)=1$, balancing statistical resolution at small $n$ against the cost of the $2^n$ brute-force reference at large $n$.

\subsection{Validation}\label{subsec:validation}

The pipeline is guarded by a small \texttt{pytest} suite, with each check mapped to a single file so that a regression points directly at the offending layer.

\paragraph{QUBO$\to$Ising round-trip (\texttt{tests/test\_ising.py}).} \texttt{test\_qubo\_cost\_matches\_portfolio\_cost} enumerates every basis state $\boldsymbol{x}\in\{0,1\}^n$ on a small instance and asserts $\boldsymbol{x}^\top Q\boldsymbol{x} + \mathrm{off} = C(\boldsymbol{x})$, while \texttt{test\_qubo\_to\_ising\_round\_trip} substitutes $\boldsymbol{z} = 1-2\boldsymbol{x}$ into the Ising form and checks it reproduces the QUBO energy. A third test verifies that the $k$-th entry of the diagonal \texttt{build\_HC\_diag} agrees with the Ising energy of bitstring $k$. Together these catch the silent sign-flip bugs in the $\{0,1\}\!\leftrightarrow\!\{-1,+1\}$ change of variables.

\paragraph{Classical baselines (\texttt{tests/test\_classical.py}).} \texttt{test\_brute\_force\_feasible} asserts that the brute-force solution is binary and satisfies $\sum_i x_i = K$; \texttt{test\_brute\_force\_is\_lower\_bound} runs greedy-Sharpe, Markowitz-rounded and simulated annealing and confirms $C_{\text{BF}} \leq C_{\text{solver}}$ up to $10^{-12}$, pinning down that \texttt{eval\_cost} is the single ground-truth oracle. \texttt{test\_run\_all\_keys} fixes the dictionary of solver names exposed by \texttt{run\_all}.

\paragraph{QAOA energy and probabilities (\texttt{tests/test\_qaoa.py}).} \texttt{test\_qaoa\_energy\_matches\_C\_units} asserts \texttt{H\_C.min()} equals the brute-force cost on a small instance, ruling out unit and sign mismatches between $H_C$ and $C(\boldsymbol{x})$. \texttt{test\_qaoa\_uniform\_state\_energy\_is\_mean\_HC} checks that $\langle +|^{\otimes n} H_C |+\rangle^{\otimes n} = \mathrm{mean}(H_C)$ at $(\gamma,\beta)=(0,0)$, confirming that the statevector kernel evaluates the right expectation. \texttt{test\_qaoa\_high\_depth\_reaches\_optimum} requires the scaled approximation ratio to satisfy $r\geq 0.95$ on $n=4$ at $p=10$ with fifteen restarts, which would fail if either the cost Hamiltonian or the mixer were mis-implemented. Together these checks pin down the cost function, the Hamiltonian, and the reported metric to the same numerical reference, leaving the QAOA results in \cref{section:result} to be read as statements about the optimiser and the ansatz rather than about the bookkeeping.
