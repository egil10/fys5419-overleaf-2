\section{Results}\label{section:result}

\subsection{QUBO Formulation and Penalty Calibration}

\textit{[Placeholder: report the chosen penalty value $\lambda$ and show that the budget
constraint $\mathbf{1}^T x = B$ is satisfied in the optimal solution for each regime.
A table of QUBO matrix sparsity and eigenvalue spread for each crisis period would fit
here.]}

\subsection{QAOA Performance vs.\ Circuit Depth}

\textit{[Placeholder: plot the approximation ratio $r = F_p / F_{\mathrm{opt}}$ as a
function of circuit depth $p \in \{1,\ldots,5\}$ for each asset universe and each crisis
regime. Discuss whether increasing $p$ yields diminishing returns.]}

\subsection{Portfolio Composition Under Crisis Regimes}

\textit{[Placeholder: show the selected $K$-asset portfolios and their realized
out-of-sample volatility for each of the three historical stress periods. A
side-by-side comparison of the QAOA portfolio and the brute-force optimum would be
instructive.]}

\subsection{Classical Benchmark Comparison}

\textit{[Placeholder: compare QAOA solution quality against (i) exact brute-force
search and (ii) a classical greedy heuristic as a function of $n$ (number of assets).
Discuss the crossover point at which QAOA becomes competitive.]}
