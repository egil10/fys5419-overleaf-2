\section{Appendix}
\label{section:appendix}

\subsection*{A. Notation and conventions}\label{subsec:app_notation}

Symbols are used consistently throughout the report, in line with \textcite{iso80000_2}. Vectors are typeset bold lower case ($\boldsymbol{x}$, $\boldsymbol\mu$), matrices bold upper case ($\boldsymbol\Sigma$, $Q$), and components carry an index ($x_i$, $\mu_i$, $\Sigma_{ij}$). Operators acting on the qubit Hilbert space carry a hat ($\hat{Z}_i$, $\hat{X}_i$); the cost and mixer Hamiltonians $H_C, H_M$ are written without hats because they are sums of hatted single-qubit operators and the typographical distinction is unambiguous in context. \Cref{tab:notation} collects every recurring symbol with its definition and the equation or section in which it is introduced.

\begin{table}[H]
    \centering
    \small
    \caption{Recurring symbols. The right-hand column points to the equation or section where the symbol is introduced.}
    \label{tab:notation}
    \begin{tabular}{@{}lll@{}}
        \toprule
        Symbol & Meaning & Defined at \\
        \midrule
        $n$                       & number of candidate assets / qubits                         & \cref{subsec:meanvar} \\
        $K$                       & target cardinality, $\sum_i x_i = K$                        & \cref{subsec:meanvar} \\
        $\boldsymbol{x} \in \{0,1\}^n$ & binary selection vector                                & \cref{subsec:meanvar} \\
        $\boldsymbol{z} \in \{-1,+1\}^n$ & spin image of $\boldsymbol{x}$, $z_i = 1-2x_i$       & \cref{subsec:ising} \\
        $P_i(t)$                  & adjusted close price of asset $i$ at time $t$               & \eqref{eq:logreturn} \\
        $r_i(t)$                  & log return of asset $i$                                     & \eqref{eq:logreturn} \\
        $\boldsymbol\mu \in \mathbb{R}^n$ & expected-return vector, $\mu_i = \langle r_i\rangle_t$ & \cref{subsec:meanvar} \\
        $\boldsymbol\Sigma \in \mathbb{R}^{n\times n}$ & return covariance matrix                  & \cref{subsec:meanvar} \\
        $\lambda \geq 0$          & risk-aversion coefficient                                   & \cref{subsec:meanvar} \\
        $A > 0$                   & budget-penalty coefficient                                  & \cref{subsec:meanvar} \\
        $C(\boldsymbol{x})$       & penalised mean--variance cost                               & \eqref{eq:Cx} \\
        $Q$                       & QUBO matrix, entries $Q_{ii}, Q_{ij}$                       & \eqref{eq:Qcoeffs} \\
        $h_i, J_{ij}, c$          & Ising linear, coupling and constant terms                   & \eqref{eq:hJ}, \eqref{eq:c} \\
        $H(\boldsymbol{z})$       & classical Ising energy                                      & \eqref{eq:Hz} \\
        $\hat Z_i, \hat X_i, \hat Y_i$ & single-qubit Pauli operators on site $i$               & \cref{subsec:pauli} \\
        $H_C$                     & quantum cost Hamiltonian, eigenvalues $C(\boldsymbol{x})$   & \eqref{eq:HC} \\
        $H_M$                     & transverse-field mixer Hamiltonian $\sum_i \hat X_i$        & \eqref{eq:HM-transverse} \\
        $H_M^{(XY)}$              & constraint-preserving XY-ring mixer                         & \eqref{eq:HM-XY} \\
        $p$                       & QAOA circuit depth                                          & \cref{subsec:qaoa} \\
        $\boldsymbol\gamma, \boldsymbol\beta \in \mathbb{R}^p$ & variational angles                  & \eqref{eq:multilayer} \\
        $|+\rangle^{\otimes n}$   & initial uniform superposition                               & \eqref{eq:plus-state} \\
        $|\psi(\boldsymbol\gamma,\boldsymbol\beta)\rangle$ & QAOA variational state                  & \eqref{eq:multilayer} \\
        $E(\boldsymbol\gamma,\boldsymbol\beta)$ & QAOA energy expectation                           & \eqref{eq:Egb} \\
        $E_{\text{opt}}, E_{\text{worst}}$ & extreme eigenvalues of $H_C$                         & \cref{subsec:metrics} \\
        $\boldsymbol{x}^\star$    & brute-force optimum, $\arg\min_{\boldsymbol{x}} C(\boldsymbol{x})$ & \eqref{eq:popt} \\
        $r$                       & scaled approximation ratio in $[0,1]$                       & \eqref{eq:approxratio} \\
        $\mathrm{gap}_{\text{rel}}$ & unscaled relative gap to the optimum                      & \eqref{eq:gaprel} \\
        $p_{\text{opt}}$          & top-1 success probability                                   & \eqref{eq:popt} \\
        $p_{\text{feas}}$         & total mass on budget-feasible bitstrings                    & \eqref{eq:pfeas} \\
        \bottomrule
    \end{tabular}
\end{table}

Unless a caption says otherwise, every numerical experiment in \cref{section:result} uses the \emph{canonical instance} fixed by \texttt{portfolio.DEFAULTS}: $n=16$, $K=4$, $\lambda=2.0$, $A=0.5$, $p=3$, ten random COBYLA multi-starts, master seed $42$. Qubit $0$ is taken to be the most significant bit of the computational-basis index, matching the order in which the QUBO matrix is built and the Ising spins are addressed in \texttt{ising.py}. Log axes are used in figures where the underlying quantity spans more than a single decade (runtime, $1-r$, $p_{\text{opt}}$); linear axes are used otherwise.


\subsection*{B. Exploratory data}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_prices.pdf}
    \caption{Normalised prices for the 16-equity universe (mag7 + quantum + quantum\_big + anti).}
    \label{fig:app_all_prices}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_returns.pdf}
    \caption{Daily log-returns, per asset, overlaid.}
    \label{fig:app_all_returns}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_rolling_vol.pdf}
    \caption{30-day rolling annualised volatility.}
    \label{fig:app_all_rolling_vol}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_histogram.pdf}
    \caption{Return distributions per asset.}
    \label{fig:app_all_histogram}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_risk_return.pdf}
    \caption{Annualised risk--return scatter for the full universe.}
    \label{fig:app_all_risk_return}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/eda/all_correlation.pdf}
    \caption{Return correlation matrix, leading-eigenvector seriated.}
    \label{fig:app_all_correlation}
\end{figure}


\subsection*{C. Motivational figures}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/visuals/markowitz_frontier.pdf}
    \caption{Continuous Markowitz efficient frontier on the mag7 sub-basket ($n=4$), with random portfolios sampled on the simplex (grey cloud), the analytical frontier (red), the capital allocation line through the tangency portfolio (ochre dotted), and per-asset annualised risk/return markers.}
    \label{fig:app_markowitz}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/visuals/cardinality_constrained.pdf}
    \caption{The cardinality constraint discretises the problem: binary equal-weighted selections form a finite set of $2^n-1$ points coloured by selection size $K$, overlaid on the continuous frontier.}
    \label{fig:app_cardinality}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/visuals/combinatorial_wall.pdf}
    \caption{The combinatorial wall: $2^n$ bitstrings on a log axis, with annotated time-budgets at one microsecond per evaluation --- the motivation for a heuristic such as QAOA.}
    \label{fig:app_combinatorial}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/visuals/cost_landscape.pdf}
    \caption{Cost landscape: all $2^n$ bitstring costs sorted in increasing order, with budget-feasible states ($\sum_i x_i = K$) highlighted in red.}
    \label{fig:app_cost_landscape}
\end{figure}


\subsection*{D. QAOA diagnostics on the canonical instance}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/qaoa/single_run_universe_p3.pdf}
    \caption{Single QAOA run on the canonical universe ($n=16$, $K=4$, $p=3$, $\lambda=2.0$, $A=0.5$, $10$ COBYLA multi-starts). Left: converged energy per restart; the spread illustrates the non-convex variational landscape and how often the optimiser gets stuck above the brute-force optimum $C(\boldsymbol{x}^\star)$. Right: probability mass on the top-40 of $1820$ budget-feasible bitstrings, against a uniform baseline of $1/2^{16}$.}
    \label{fig:app_qaoa_single}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/analysis/universe_p1_landscape.pdf}
    \caption{QAOA energy landscape $E(\gamma,\beta)$ at $p=1$ on the canonical universe ($n=16$, $K=4$, $\lambda=2.0$, $A=0.5$). Bright contours mark high-energy basins; dark corners are low-energy minima; the red star marks the best of ten COBYLA multi-starts at $E = 0.6456$.}
    \label{fig:app_landscape}
\end{figure}


\subsection*{E. Sweep results}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/compare/depth_sweep.pdf}
    \caption{Sweep 1 --- QAOA circuit depth. Canonical instance ($n=16$, $K=4$, $\lambda=2.0$, $A=0.5$, $10$ multi-starts) with $p \in \{1,\dots,5\}$. Left: scaled approximation ratio $r=(E_{\text{worst}}-E)/(E_{\text{worst}}-E_{\text{opt}})$; the dashed red line marks the brute-force optimum at $r=1$. Right: top-1 success probability and feasibility probability, against the uniform-sampling baseline at $1/2^{16}$.}
    \label{fig:app_depth_sweep}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/compare/penalty_sweep.pdf}
    \caption{Sweep 4 --- budget-penalty calibration. Canonical instance with $p=3$ and $A \in \{0.5, 2, 8, 32\}$. Left: scaled ratio vs $A$ (log axis). Right: top-1 and feasibility probabilities; the strong non-monotonicity in feasibility reflects optimiser variance at fixed multi-start count rather than a genuine $A$-dependence.}
    \label{fig:app_penalty_sweep}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/compare/risk.pdf}
    \caption{Sweep 3 --- risk aversion. Scaled ratio vs $\lambda \in [10^{-1}, 10^2]$ (seven log-spaced values) at $p=3$. Brute force, greedy-Sharpe, Markowitz-round and simulated annealing all sit at $r\approx 1$ and are visually overlaid; QAOA (red) stays in the $r \approx 0.93$--$0.97$ band across the full sweep.}
    \label{fig:app_risk_sweep}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/compare/scaling.pdf}
    \caption{Sweep 2 --- size scaling. Median over random size-$n$ subsets of the canonical universe ($M=20$ at $n\!\leq\!10$, $M=8$ at $n=12$, $M=4$ at $n=14$, $M=1$ at $n=16$); shaded bands show interquartile range across subsets. Left: scaled ratio per solver vs $n$. Right: median wall-clock runtime on a log scale; QAOA grows roughly as $2^n$ while the classical baselines stay essentially flat.}
    \label{fig:app_scaling}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{plots/compare/scaling_zoom.pdf}
    \caption{Sweep 2 (zoom). Left: same scaled ratio as Fig.~\ref{fig:app_scaling}, restricted to $r \in [0.93, 1.00]$ so the QAOA band is visible against the classical solvers that all sit at machine precision. Right: $1-r$ on a log axis, making the QAOA-vs-classical gap explicit; classical solvers floor near floating-point zero, while QAOA stays $10^{-2}$ to $10^{-1}$ above the optimum.}
    \label{fig:app_scaling_zoom}
\end{figure}


\subsection*{F. Background literature}

The reference list collected during this project covers several overlapping themes; every bibliography entry is cited at least once here so that the reference list can be audited and pruned. Each citation is given individually rather than bundled, so that an author-year reader can match line by line.

\paragraph{Foundational quantum mechanics.}
The original EPR paper \parencite{epr1935} sets the historical context for entanglement, which underpins the interference mechanism exploited by QAOA. Early demonstrations of small-scale quantum computation on molecular spin systems were reported by \textcite{gershenfeld_qc_molecules}.

\paragraph{Quantum computing textbooks.}
General textbook references include the foundational \textcite{nielsen_chuang}, the applied treatment of \textcite{hidary_qc_applied}, the programmer-oriented exposition of \textcite{hundt_qc_programmers}, the formal mathematics of \textcite{scherer_math_qc}, the introductory text of \textcite{olivares_student_qc}, the early survey of \textcite{rieffel1998introqc}, and the Qiskit-flavoured course of \textcite{loredo_learn_qc_python_ibm}. Quantum machine learning specifically is covered by \textcite{schuld_qml}, \textcite{conti_qml} and \textcite{pattanayak_qml_python}.

\paragraph{QAOA and the variational paradigm.}
The QAOA algorithm itself is due to \textcite{farhi2014qaoa}, with the depth and landscape analysis of \textcite{zhou2018qaoa}. Pedagogical introductions are given by \textcite{giovagnoli_qaoa_intro}, by \textcite{irfan_qaoa_simulators} for the hardware-vs-simulator comparison, and by the IBM Quantum tutorial \parencite{ibm_qaoa_tutorial}. The closely related variational quantum eigensolver framework is reviewed by \textcite{tilly2022vqe}.

\paragraph{Many-body solvable models.}
The Lipkin--Meshkov--Glick model \parencite{lipkin1965} provides a small, exactly solvable many-body Hamiltonian routinely used as a VQE benchmark in the FYS5419 course.

\paragraph{Markowitz and quantum portfolio optimisation.}
Modern portfolio theory originates with \textcite{markowitz1952}; statistical-learning context for return estimation is found in \textcite{hastie}. Recent quantum approaches to constrained portfolio selection are reported by \textcite{knapsack2024qaoa} for a knapsack formulation, by \textcite{thomassin2025slack} for slack variables, by \textcite{mancilla2026constrained} for XY-mixers, by \textcite{stopfer2025benchmark} for a systematic benchmark, by \textcite{shen2025noisy} for noisy-device simulations, by \textcite{morapakula2025annealing} for annealing comparisons, by \textcite{soloviev2025pauli} for a Pauli-correlation encoding, and by \textcite{soloviev2025cutting} for circuit-cutting techniques.

\paragraph{Notation and standards.}
Mathematical notation conventions follow the ISO 80000-2 standard \parencite{iso80000_2}.

\paragraph{Software libraries.}
The implementations consulted while developing the code base include Qiskit \parencite{qiskit}, PennyLane \parencite{pennylane}, QuTiP \parencite{qutip} with its source on GitHub \parencite{qutip_github}, PySCF \parencite{pyscf}, and the qBraid platform \parencite{qbraid}.

\paragraph{Course teaching material.}
The full set of FYS5419 weekly teaching material, hand-written notes, and lecture videos is hosted in the public course repository maintained by \textcite{qcm2026_coursepage}; supplementary workshop material is provided by \textcite{quantgates_workshop}.
