\section{Appendix}
\label{section:appendix}

\subsection*{A. Notation and conventions}\label{subsec:app_notation}

Symbols are used consistently throughout the report, in line with \textcite{iso80000_2}. Vectors are typeset bold lower case ($\boldsymbol{x}$, $\boldsymbol\mu$), matrices bold upper case ($\boldsymbol\Sigma$, $Q$), and components carry an index ($x_i$, $\mu_i$, $\Sigma_{ij}$). Operators acting on the qubit Hilbert space carry a hat ($\hat{Z}_i$, $\hat{X}_i$); the cost and mixer Hamiltonians $H_C, H_M$ are written without hats because they are sums of hatted single-qubit operators and the typographical distinction is unambiguous in context. \Cref{tab:notation} collects every recurring symbol with its definition and the equation or section in which it is introduced.

\begin{table}[H]
    \centering
    \small
    \caption{Recurring symbols. The right-hand column points to the equation or section where the symbol is introduced.}
    \label{tab:notation}
    \begin{tabular}{@{}lll@{}}
        \toprule
        Symbol & Meaning & Defined at \\
        \midrule
        $n$                       & number of candidate assets / qubits                         & \cref{subsec:meanvar} \\
        $K$                       & target cardinality, $\sum_i x_i = K$                        & \cref{subsec:meanvar} \\
        $\boldsymbol{x} \in \{0,1\}^n$ & binary selection vector                                & \cref{subsec:meanvar} \\
        $\boldsymbol{z} \in \{-1,+1\}^n$ & spin image of $\boldsymbol{x}$, $z_i = 1-2x_i$       & \cref{subsec:ising} \\
        $P_i(t)$                  & adjusted close price of asset $i$ at time $t$               & \eqref{eq:logreturn} \\
        $r_i(t)$                  & log return of asset $i$                                     & \eqref{eq:logreturn} \\
        $\boldsymbol\mu \in \mathbb{R}^n$ & expected-return vector, $\mu_i = \langle r_i\rangle_t$ & \cref{subsec:meanvar} \\
        $\boldsymbol\Sigma \in \mathbb{R}^{n\times n}$ & return covariance matrix                  & \cref{subsec:meanvar} \\
        $\lambda \geq 0$          & risk-aversion coefficient                                   & \cref{subsec:meanvar} \\
        $A > 0$                   & budget-penalty coefficient                                  & \cref{subsec:meanvar} \\
        $C(\boldsymbol{x})$       & penalised mean--variance cost                               & \eqref{eq:Cx} \\
        $Q$                       & QUBO matrix, entries $Q_{ii}, Q_{ij}$                       & \eqref{eq:Qcoeffs} \\
        $h_i, J_{ij}, c$          & Ising linear, coupling and constant terms                   & \eqref{eq:hJ}, \eqref{eq:c} \\
        $H(\boldsymbol{z})$       & classical Ising energy                                      & \eqref{eq:Hz} \\
        $\hat Z_i, \hat X_i, \hat Y_i$ & single-qubit Pauli operators on site $i$               & \cref{subsec:pauli} \\
        $H_C$                     & quantum cost Hamiltonian, eigenvalues $C(\boldsymbol{x})$   & \eqref{eq:HC} \\
        $H_M$                     & transverse-field mixer Hamiltonian $\sum_i \hat X_i$        & \eqref{eq:HM-transverse} \\
        $H_M^{(XY)}$              & constraint-preserving XY-ring mixer                         & \eqref{eq:HM-XY} \\
        $p$                       & QAOA circuit depth                                          & \cref{subsec:qaoa} \\
        $\boldsymbol\gamma, \boldsymbol\beta \in \mathbb{R}^p$ & variational angles                  & \eqref{eq:multilayer} \\
        $|+\rangle^{\otimes n}$   & initial uniform superposition                               & \eqref{eq:plus-state} \\
        $|\psi(\boldsymbol\gamma,\boldsymbol\beta)\rangle$ & QAOA variational state                  & \eqref{eq:multilayer} \\
        $E(\boldsymbol\gamma,\boldsymbol\beta)$ & QAOA energy expectation                           & \eqref{eq:Egb} \\
        $E_{\text{opt}}, E_{\text{worst}}$ & extreme eigenvalues of $H_C$                         & \cref{subsec:metrics} \\
        $\boldsymbol{x}^\star$    & brute-force optimum, $\arg\min_{\boldsymbol{x}} C(\boldsymbol{x})$ & \eqref{eq:popt} \\
        $r$                       & scaled approximation ratio in $[0,1]$                       & \eqref{eq:approxratio} \\
        $\mathrm{gap}_{\text{rel}}$ & unscaled relative gap to the optimum                      & \eqref{eq:gaprel} \\
        $p_{\text{opt}}$          & top-1 success probability                                   & \eqref{eq:popt} \\
        $p_{\text{feas}}$         & total mass on budget-feasible bitstrings                    & \eqref{eq:pfeas} \\
        \bottomrule
    \end{tabular}
\end{table}

Unless a caption says otherwise, every numerical experiment in \cref{section:result} uses the \emph{canonical instance}: $n=16$, $K=4$, $\lambda=2.0$, $A=0.5$, $p=3$, ten random COBYLA multi-starts, master seed $42$. Qubit $0$ is taken to be the most significant bit of the computational-basis index, matching the order in which the QUBO matrix is built and the Ising spins are addressed. Log axes are used in figures where the underlying quantity spans more than a single decade (runtime, $1-r$, $p_{\text{opt}}$); linear axes are used otherwise.


\clearpage
\subsection*{B. Exploratory data}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \captionsetup[subfigure]{font=scriptsize,skip=1pt}
    \setlength{\abovecaptionskip}{2pt}
    \setlength{\belowcaptionskip}{0pt}

    \begin{subfigure}[t]{0.82\textwidth}
        \centering
        \includegraphics[width=\linewidth,height=0.255\textheight,keepaspectratio]{plots/eda/all_returns.pdf}
        \caption{Daily log-returns $r_i(t)=\ln P_i(t)/P_i(t-1)$ per asset. The clustered spikes in the quantum basket panels reflect the high idiosyncratic volatility of IONQ, RGTI, QBTS and QUBT.}
        \label{fig:app_all_returns}
    \end{subfigure}

    \vspace{0.35em}

    \begin{subfigure}[t]{0.82\textwidth}
        \centering
        \includegraphics[width=\linewidth,height=0.255\textheight,keepaspectratio]{plots/eda/all_rolling_vol.pdf}
        \caption{Thirty-day rolling annualised volatility $\sqrt{252}\,\sigma_{30}(r_i)$. The small-cap quantum names sit persistently above the mag7 and quantum-adjacent baskets.}
        \label{fig:app_all_rolling_vol}
    \end{subfigure}

    \vspace{0.35em}

    \begin{subfigure}[t]{0.82\textwidth}
        \centering
        \includegraphics[width=\linewidth,height=0.255\textheight,keepaspectratio]{plots/eda/all_histogram.pdf}
        \caption{Empirical distribution of daily log-returns. The mag7 panels are sharply peaked near zero, while the quantum-basket histograms are visibly broader and fatter-tailed.}
        \label{fig:app_all_histogram}
    \end{subfigure}

    \caption{\textbf{Exploratory return diagnostics for the sixteen-equity universe.}
    Panels use 751 daily observations from 2023-01-03 to 2025-12-30. The three diagnostics show the same core structure: the small-cap quantum basket has materially higher idiosyncratic volatility and fatter return tails than the mega-cap and quantum-adjacent assets.}
    \label{fig:app_eda_three_rows}
\end{figure}

\clearpage

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.72\linewidth,
        height=0.245\textheight,
        keepaspectratio
    ]{plots/eda/all_correlation.pdf}
    \caption{Pearson return-correlation matrix $\boldsymbol\Sigma_{ij}/\sqrt{\boldsymbol\Sigma_{ii}\boldsymbol\Sigma_{jj}}$ across all sixteen tickers, reordered by the leading eigenvector. The dense block corresponds to the mega-cap technology cluster, while the anti and small-cap quantum baskets form weaker co-movement blocks.}
    \label{fig:app_all_correlation}
\end{figure}

\vspace{-0.5em}

\subsection*{C. Motivational figures}

\vspace{-0.5em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.72\linewidth,
        height=0.245\textheight,
        keepaspectratio
    ]{plots/visuals/markowitz_frontier.pdf}
    \caption{Continuous Markowitz efficient frontier on the mag7 sub-basket ($n=4$: AAPL, MSFT, GOOGL, AMZN). Random simplex-sampled portfolios form the cloud, the analytical efficient frontier traces the feasible upper envelope, and the labelled markers give per-asset annualised risk and return.}
    \label{fig:app_markowitz}
\end{figure}

\vspace{-0.5em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.72\linewidth,
        height=0.245\textheight,
        keepaspectratio
    ]{plots/visuals/cardinality_constrained.pdf}
    \caption{The cardinality constraint discretises the mag7 frontier ($n=4$). Binary equal-weighted selections form a finite set of $2^n-1=15$ points, with only the $\binom{n}{K}$ points at the prescribed budget admissible for the cardinality-constrained problem.}
    \label{fig:app_cardinality}
\end{figure}
\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/visuals/combinatorial_wall.pdf}
    \caption{The combinatorial wall on the canonical mag7-extended instance: $2^n$ bitstrings on a log vertical axis as $n$ grows, with annotated wall-clock budgets at one microsecond per evaluation. At $n=16$ the brute force is still tractable, motivating the use of a heuristic such as QAOA for larger regimes.}
    \label{fig:app_combinatorial}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/visuals/cost_landscape.pdf}
    \caption{Cost landscape on the canonical instance ($n=16$, $K=4$, $\lambda=2.0$, $A=0.5$): all $2^{16}=65\,536$ bitstring costs sorted in increasing order. Budget-feasible states with $\sum_i x_i = K$ are highlighted in red; the feasible bitstrings are scattered through the spectrum rather than clustered.}
    \label{fig:app_cost_landscape}
\end{figure}

\vspace{-0.4em}

\subsection*{D. QAOA diagnostics on the canonical instance}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/qaoa/single_run_universe_p3.pdf}
    \caption{Single QAOA run on the canonical universe ($n=16$, $K=4$, $p=3$, $\lambda=2.0$, $A=0.5$, $10$ COBYLA multi-starts). Left: converged energy per restart. Right: probability mass on the top budget-feasible bitstrings against a uniform baseline. At this restart budget: $r=0.945$, $\mathrm{gap}=2847$, $p_{\text{opt}}=1.6\times 10^{-4}$, $p_{\text{feas}}=0.31$.}
    \label{fig:app_qaoa_single}
\end{figure}
\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/analysis/cost_spectrum.pdf}
    \caption{Cost spectrum over all $2^{16}=65{,}536$ bitstrings of the canonical instance, colour-coded by feasibility. Vertical lines mark the brute-force optimum, the converged QAOA expectation, and the all-ones-state cost.}
    \label{fig:app_cost_spectrum}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/analysis/hamming_weight_mass.pdf}
    \caption{QAOA probability mass by Hamming weight on the canonical instance ($n=16$, $K=4$, $p=3$). The QAOA distribution roughly tracks the binomial baseline, with only mild enhancement at the target weight $K=4$.}
    \label{fig:app_hamming_mass}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/analysis/universe_p1_landscape.pdf}
    \caption{QAOA energy landscape $E(\gamma,\beta)$ at $p=1$ on the canonical universe. The red star marks the best of ten COBYLA multi-starts.}
    \label{fig:app_landscape}
\end{figure}
\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/analysis/universe_p1_landscape_with_restarts.pdf}
    \caption{Same $p=1$ energy landscape as \cref{fig:app_landscape}, with all ten COBYLA restart endpoints overlaid. The endpoints cluster tightly inside the same lower-right basin, indicating that the multi-start budget is effectively saturated already at $p=1$.}
    \label{fig:app_landscape_restarts}
\end{figure}

\vspace{-0.4em}

\subsection*{E. Sweep results}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/compare/depth_sweep.pdf}
    \caption{Sweep 1: QAOA circuit depth under random multi-start on the canonical instance ($n=16$, $K=4$, $\lambda=2.0$, $A=0.5$, $50$ restarts) for $p \in \{1,\dots,5\}$. The non-monotonic dip at higher depths reflects the angle-space/restart-budget mismatch.}
    \label{fig:app_depth_sweep}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/compare/depth_warm_vs_random.pdf}
    \caption{Sweep 1 extension: warm-start versus random multi-start QAOA on the canonical instance. Warm starts restore roughly monotone depth scaling and substantially improve feasibility mass relative to random restarts.}
    \label{fig:app_depth_warm}
\end{figure}
\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/compare/penalty_sweep.pdf}
    \caption{Sweep 4: budget-penalty calibration on the canonical instance at $p=3$. Feasibility peaks near $A=0.32$ and then decays as the penalty begins to flatten the feasible-subspace cost surface.}
    \label{fig:app_penalty_sweep}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/compare/risk.pdf}
    \caption{Sweep 3: risk aversion. Scaled ratio versus $\lambda$ at $p=3$. The classical baselines remain essentially optimal throughout, while QAOA stays in a narrow $r\approx 0.97$--$0.99$ band.}
    \label{fig:app_risk_sweep}
\end{figure}

\vspace{-0.4em}

\begin{figure}[H]
    \centering
    \captionsetup{font=scriptsize}
    \includegraphics[
        width=0.78\textwidth,
        height=0.255\textheight,
        keepaspectratio
    ]{plots/compare/scaling.pdf}
    \caption{Sweep 2: size scaling. Left: scaled ratio per solver versus $n$. Right: median wall-clock runtime on a log scale; QAOA grows roughly as $2^n$ while the classical baselines remain nearly flat.}
    \label{fig:app_scaling}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{plots/compare/scaling_zoom.pdf}
    \caption{Sweep 2 (zoom). Left: same scaled ratio as Fig.~\ref{fig:app_scaling}, restricted to $r \in [0.93, 1.005]$ so the QAOA band is visible against the classical solvers that all sit at machine precision. Right: $1-r$ on a log axis, making the QAOA-vs-classical gap explicit; classical solvers floor near floating-point zero, while QAOA stays $10^{-2}$ to $10^{-1}$ above the optimum.}
    \label{fig:app_scaling_zoom}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\linewidth]{plots/compare/xy_mixer.pdf}
    \caption{Sweep 5: constraint-preserving XY-ring mixer with Dicke initial state, benchmarked against the standard transverse-field mixer with uniform $|+\rangle^{\otimes n}$ start. Sub-instance: $n=8$, $K=2$, $\lambda=2.0$ (penalty $A=0.5$ active for the standard protocol; ignored by XY-Dicke). Twenty random COBYLA restarts per depth, $p\in\{1,2,3,4\}$. Left: scaled ratio versus depth; XY-Dicke is flat at $r\approx 0.9995$ across all depths, while standard $r$ peaks at $0.992$ and dips at $p=4$. Centre: feasibility probability; XY-Dicke is at $p_{\text{feas}}=1$ by construction, standard fluctuates in $[0.47, 0.81]$. Right: wall-clock runtime per solve on a log scale; XY-Dicke is roughly $5\times$ slower per layer because \texttt{expm\_multiply} replaces the factorised single-qubit rotations.}
    \label{fig:app_xy_mixer}
\end{figure}


